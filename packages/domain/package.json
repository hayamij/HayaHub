{
  "name": "hayahub-domain",
  "version": "1.0.0",
  "private": true,
  "description": "Domain layer - Core business entities and rules",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "hayahub-shared": "file:../shared"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
