{
  "name": "hayahub-shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utilities and types",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "echo \"No tests in shared package (utilities only)\" && exit 0",
    "test:watch": "echo \"No tests in shared package\" && exit 0",
    "test:ui": "echo \"No tests in shared package\" && exit 0"
  },
  "devDependencies": {
    "@vitest/ui": "^4.0.16",
    "typescript": "^5.3.0",
    "vitest": "^4.0.16"
  }
}
