{
  "name": "hayahub-business",
  "version": "1.0.0",
  "private": true,
  "description": "Business layer - Use cases and application logic",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "hayahub-domain": "file:../domain",
    "hayahub-shared": "file:../shared"
  },
  "devDependencies": {
    "@vitest/ui": "^4.0.16",
    "typescript": "^5.3.0",
    "vitest": "^4.0.16"
  }
}
