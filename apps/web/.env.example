# GitHub API Configuration
# Required for GitHub sync feature (optional - app works with localStorage only if not configured)
# 
# To enable GitHub storage:
# 1. Copy this file to .env.local
# 2. Create a GitHub Personal Access Token with 'repo' scope
# 3. Fill in your values below

# GitHub Personal Access Token (requires 'repo' scope)
NEXT_PUBLIC_GITHUB_TOKEN=your_github_token_here

# GitHub repository information
NEXT_PUBLIC_GITHUB_OWNER=your_github_username
NEXT_PUBLIC_GITHUB_REPO=your_repo_name

# Branch to store data (default: 'data')
NEXT_PUBLIC_GITHUB_BRANCH=data
